import React from 'react';
interface ListProps {
    result:object
}
const List = (props:ListProps) => {
    const {result} = props
    return (
        <div>
            
        </div>
    );
};

export default List;