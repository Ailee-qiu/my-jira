import { Button } from 'antd';
import React from 'react';

const BExportExcel = () => {
    return (
        <div>
            <Button type="primary" ghost>
                导出
            </Button>
            
        </div>
    );
};

export default BExportExcel;